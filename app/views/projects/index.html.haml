%h1 Project List
= link_to("Submit a project", new_project_path)
- if @projects.empty?
  %p No projects have been submitted this week.
- else
  %ul
    - @projects.each do |project|
      %li= project.title
